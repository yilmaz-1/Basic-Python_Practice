s="12536" # burada bir değişken tanımlanıyor. karakter dizini olarak .string ifade olarak.
f=s.isdigit() # yukarıda string olarak tanımlanan değişken ifadenin tamamı sayısal verilerden oluşup oluşmadığı
# isdigit() metodu ile kontrol ediliyor
print(f) # f değişkeni ekrana yazdırılıyor.

m="12536fgh" # burada string ifade tanımlanıyor m değişkeni olarak.
h=m.isdigit() # tanımlana m değişkeni isdigit metodu ile bütün karakter dizini harflerden mi oluşup oluşmadığı
# kontrol ediliyor.
print(h) # ekrana h değişkeni print ile yazdırılıyor.

q="12536 " # burada bir değişken tanımlanıyor karakter dizini olarka stirng ifade olarak
l=q.isdigit() # yukarıda tanımlanan değişken isdigit metodu ile kontorl ediliyor bütün elemanları sayısal bir değer mi diye.
print(l) # l değişkeni print ile ekrana yazdırılıyor.

